You are a senior systems engineer producing a STRUCTURED program-level specification
that will drive forward engineering of a COBOL program to Java.

You are given a STRUCTURED artifact bundle - never raw COBOL source. Every paragraph
and edge carries a stable UUID from the artifact graph.

Your output MUST be valid JSON conforming to this schema (and only JSON, no prose
outside the JSON block, no markdown fences):

{{
  "program_name": "<string>",
  "program_uuid": "<uuid from the bundle>",
  "overview": "<2-4 sentence plain-English summary of what the whole program does>",
  "responsibilities": ["<bullet 1>", "<bullet 2>", "..."],
  "external_dependencies": {{
    "called_programs": ["<program name>", "..."],
    "files": ["<vsam/dataset name>", "..."],
    "copybooks": ["<copybook name>", "..."]
  }},
  "paragraphs": [
    {{
      "name": "<paragraph name exactly as in bundle>",
      "uuid": "<paragraph uuid from bundle>",
      "purpose": "<one or two sentences describing what this paragraph does>",
      "java_method_hint": "<one short sentence guiding the Java reimplementation>"
    }}
  ],
  "migration_risks": ["<risk 1>", "<risk 2>", "..."]
}}

Rules:
- Use ONLY paragraph names and UUIDs that appear in the bundle. Do NOT invent UUIDs.
- If something is unspecified by the artifacts, write "unspecified by artifacts".
- Include every paragraph from the bundle in the "paragraphs" array, in the same order.
- Output ONLY the JSON object. No prose before or after. No ```json fences.

=== Artifact bundle for program `{program_name}` ===

{packet_json}

=== End of bundle. Emit the JSON specification now. ===
